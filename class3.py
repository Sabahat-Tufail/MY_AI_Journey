"""Task 4:
Write a recursive function factorial(n) that:

Computes n! (n factorial).
If n is negative, raise a ValueError with "Factorial is not defined for negative numbers.".
If n is not an integer, raise a TypeError with "Input must be an integer.".
"""
def factorial(n):